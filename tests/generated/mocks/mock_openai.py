"""
Centralized mocks for openai
Auto-generated by Enterprise Test Generator
"""

from typing import Any, Dict, List, Optional
from unittest.mock import AsyncMock, MagicMock, Mock, patch

import pytest


class OpenaiMock:
    """Mock factory for openai."""

    @staticmethod
    def create_mock() -> MagicMock:
        """Create a configured mock for openai."""
        mock = MagicMock()
        mock.chat.completions.create.return_value = MagicMock(choices=[MagicMock(message=MagicMock(content='response'))])
        return mock

    @staticmethod
    def create_async_mock() -> AsyncMock:
        """Create a configured async mock for openai."""
        mock = AsyncMock()
        mock.chat.completions.create.return_value = AsyncMock(choices=[AsyncMock(message=AsyncMock(content='response'))])
        return mock



@pytest.fixture
def mock_openai():
    """Fixture for openai mock."""
    return OpenaiMock.create_mock()

@pytest.fixture
def mock_openai_async():
    """Fixture for async openai mock."""
    return OpenaiMock.create_async_mock()

