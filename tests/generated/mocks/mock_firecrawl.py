"""
Centralized mocks for firecrawl
Auto-generated by Enterprise Test Generator
"""

from typing import Any, Dict, List, Optional
from unittest.mock import AsyncMock, MagicMock, Mock, patch

import pytest


class FirecrawlMock:
    """Mock factory for firecrawl."""

    @staticmethod
    def create_mock() -> MagicMock:
        """Create a configured mock for firecrawl."""
        mock = MagicMock()
        mock.scrape.return_value = {'content': 'test'}
        mock.crawl.return_value = []
        return mock

    @staticmethod
    def create_async_mock() -> AsyncMock:
        """Create a configured async mock for firecrawl."""
        mock = AsyncMock()
        mock.scrape.return_value = {'content': 'test'}
        mock.crawl.return_value = []
        return mock



@pytest.fixture
def mock_firecrawl():
    """Fixture for firecrawl mock."""
    return FirecrawlMock.create_mock()

@pytest.fixture
def mock_firecrawl_async():
    """Fixture for async firecrawl mock."""
    return FirecrawlMock.create_async_mock()

