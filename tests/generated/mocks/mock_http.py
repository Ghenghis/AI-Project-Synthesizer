"""
Centralized mocks for http
Auto-generated by Enterprise Test Generator
"""

from unittest.mock import AsyncMock, MagicMock

import pytest


class HttpMock:
    """Mock factory for http."""

    @staticmethod
    def create_mock() -> MagicMock:
        """Create a configured mock for http."""
        mock = MagicMock()
        mock.get.return_value = MagicMock(status_code=200, json=lambda: {})
        return mock

    @staticmethod
    def create_async_mock() -> AsyncMock:
        """Create a configured async mock for http."""
        mock = AsyncMock()
        mock.get.return_value = AsyncMock(status_code=200, json=lambda: {})
        return mock



@pytest.fixture
def mock_http():
    """Fixture for http mock."""
    return HttpMock.create_mock()

@pytest.fixture
def mock_http_async():
    """Fixture for async http mock."""
    return HttpMock.create_async_mock()

