"""
Centralized mocks for ollama
Auto-generated by Enterprise Test Generator
"""

from typing import Any, Dict, List, Optional
from unittest.mock import AsyncMock, MagicMock, Mock, patch

import pytest


class OllamaMock:
    """Mock factory for ollama."""

    @staticmethod
    def create_mock() -> MagicMock:
        """Create a configured mock for ollama."""
        mock = MagicMock()
        mock.generate.return_value = {'response': 'test'}
        mock.chat.return_value = {'message': {'content': 'test'}}
        return mock

    @staticmethod
    def create_async_mock() -> AsyncMock:
        """Create a configured async mock for ollama."""
        mock = AsyncMock()
        mock.generate.return_value = {'response': 'test'}
        mock.chat.return_value = {'message': {'content': 'test'}}
        return mock



@pytest.fixture
def mock_ollama():
    """Fixture for ollama mock."""
    return OllamaMock.create_mock()

@pytest.fixture
def mock_ollama_async():
    """Fixture for async ollama mock."""
    return OllamaMock.create_async_mock()

