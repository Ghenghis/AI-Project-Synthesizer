"""
Centralized mocks for elevenlabs
Auto-generated by Enterprise Test Generator
"""

from unittest.mock import AsyncMock, MagicMock

import pytest


class ElevenlabsMock:
    """Mock factory for elevenlabs."""

    @staticmethod
    def create_mock() -> MagicMock:
        """Create a configured mock for elevenlabs."""
        mock = MagicMock()
        mock.generate_audio.return_value = b'audio_data'
        mock.get_voices.return_value = []
        return mock

    @staticmethod
    def create_async_mock() -> AsyncMock:
        """Create a configured async mock for elevenlabs."""
        mock = AsyncMock()
        mock.generate_audio.return_value = b'audio_data'
        mock.get_voices.return_value = []
        return mock



@pytest.fixture
def mock_elevenlabs():
    """Fixture for elevenlabs mock."""
    return ElevenlabsMock.create_mock()

@pytest.fixture
def mock_elevenlabs_async():
    """Fixture for async elevenlabs mock."""
    return ElevenlabsMock.create_async_mock()

