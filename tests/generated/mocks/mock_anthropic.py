"""
Centralized mocks for anthropic
Auto-generated by Enterprise Test Generator
"""

from typing import Any, Dict, List, Optional
from unittest.mock import AsyncMock, MagicMock, Mock, patch

import pytest


class AnthropicMock:
    """Mock factory for anthropic."""

    @staticmethod
    def create_mock() -> MagicMock:
        """Create a configured mock for anthropic."""
        mock = MagicMock()
        mock.messages.create.return_value = MagicMock(content=[MagicMock(text='response')])
        return mock

    @staticmethod
    def create_async_mock() -> AsyncMock:
        """Create a configured async mock for anthropic."""
        mock = AsyncMock()
        mock.messages.create.return_value = AsyncMock(content=[AsyncMock(text='response')])
        return mock



@pytest.fixture
def mock_anthropic():
    """Fixture for anthropic mock."""
    return AnthropicMock.create_mock()

@pytest.fixture
def mock_anthropic_async():
    """Fixture for async anthropic mock."""
    return AnthropicMock.create_async_mock()

