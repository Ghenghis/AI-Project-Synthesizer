"""
Centralized mocks for github
Auto-generated by Enterprise Test Generator
"""

from unittest.mock import Mock, MagicMock, AsyncMock, patch
from typing import Any, Dict, List, Optional
import pytest


class GithubMock:
    """Mock factory for github."""
    
    @staticmethod
    def create_mock() -> MagicMock:
        """Create a configured mock for github."""
        mock = MagicMock()
        mock.search_repositories.return_value = []
        mock.get_repo.return_value = MagicMock()
        return mock
    
    @staticmethod
    def create_async_mock() -> AsyncMock:
        """Create a configured async mock for github."""
        mock = AsyncMock()
        mock.search_repositories.return_value = []
        mock.get_repo.return_value = AsyncMock()
        return mock



@pytest.fixture
def mock_github():
    """Fixture for github mock."""
    return GithubMock.create_mock()

@pytest.fixture
def mock_github_async():
    """Fixture for async github mock."""
    return GithubMock.create_async_mock()

