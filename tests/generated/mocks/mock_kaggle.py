"""
Centralized mocks for kaggle
Auto-generated by Enterprise Test Generator
"""

from typing import Any, Dict, List, Optional
from unittest.mock import AsyncMock, MagicMock, Mock, patch

import pytest


class KaggleMock:
    """Mock factory for kaggle."""

    @staticmethod
    def create_mock() -> MagicMock:
        """Create a configured mock for kaggle."""
        mock = MagicMock()
        mock.search_datasets.return_value = []
        mock.download_dataset.return_value = '/path/to/data'
        return mock

    @staticmethod
    def create_async_mock() -> AsyncMock:
        """Create a configured async mock for kaggle."""
        mock = AsyncMock()
        mock.search_datasets.return_value = []
        mock.download_dataset.return_value = '/path/to/data'
        return mock



@pytest.fixture
def mock_kaggle():
    """Fixture for kaggle mock."""
    return KaggleMock.create_mock()

@pytest.fixture
def mock_kaggle_async():
    """Fixture for async kaggle mock."""
    return KaggleMock.create_async_mock()

